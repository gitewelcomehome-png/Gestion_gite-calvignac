# 🚀 IDÉES VIRALES POUR LIVEOWNERUNIT
## Améliorations pour devenir la plateforme #1 de gestion de gîtes


## 🤖 1. ASSISTANT IA CONVERSATIONNEL INTÉGRÉ

### Concept
ChatBot intelligent dans l'interface qui répond aux questions et automatise les tâches.

### Bénéfices
- Gain de temps : 1-2h/jour économisées
- Support 24/7 sans coût supplémentaire
- Onboarding facilité pour nouveaux utilisateurs

### Fonctionnalités clés
- **Chat en temps réel** avec IA (coin bas droit)
- **Compréhension du contexte** : "Ajoute une réservation pour Jean Dupont du 15 au 22 mars au Gîte des Roses"
- **Suggestions proactives** : "J'ai remarqué que vous n'avez pas encore envoyé la fiche client pour la réservation de demain"
- **Analyse prédictive** : "Votre taux d'occupation pour juillet est de 65%. Voulez-vous des conseils pour l'optimiser ?"
- **Génération automatique** : 
  - Descriptions de gîtes optimisées SEO
  - Réponses aux messages clients
  - Messages de bienvenue personnalisés
- **Multilangue** : détection automatique FR/EN

### 📝 Prompt Copilot
```
Crée un ChatBot IA pour ma plateforme LiveOwnerUnit en JavaScript.

Fonctionnalités :
1. Widget chat en bas à droite (bouton flottant + fenêtre de chat)
2. Connexion à l'API Claude/ChatGPT pour réponses intelligentes
3. Contexte du système :
   - "Tu es l'assistant IA de LiveOwnerUnit, plateforme de gestion de gîtes"
   - "Tu aides les propriétaires avec leurs réservations, fiscalité, ménages, etc."
4. Actions automatiques possibles :
   - "Ajoute une réservation" → ouvre le formulaire
   - "Montre mes stats" → affiche le dashboard
   - "Génère une fiche client" → lance la génération
5. Suggestions proactives basées sur les données :
   - Fiche client non envoyée → alerte
   - Taux occupation faible → conseils
6. Historique des conversations stocké
7. Bouton "Exemples de questions" pour guider l'utilisateur

Design :
- Icône robot animée (Lucide MessageCircle)
- Bulles de chat avec animations
- Indicateur "en train d'écrire..."
- Possibilité de copier les réponses
- Dark/Light mode compatible

API à utiliser : Anthropic Claude API avec clé fournie par l'utilisateur
```

---

##  👥 PROGRAMME DE PARRAINAGE PROFESSIONNEL LIVEOWNERUNIT

## Concept
Système de parrainage avec **réductions progressives** basées sur le nombre de filleuls actifs et payants.

---

## ✅ Légalité
- **100% légal** : Réductions tarifaires = remises commerciales autorisées  
- **Pas de système pyramidal** : Plafonné à 20 filleuls  
- **Transparence** : Conditions claires affichées  
- **Déclarable** : Avantages commerciaux légitimes

---

## 💰 Fonctionnement du Système

### Réduction Progressive
- **Chaque filleul actif payant = -5%** sur l'abonnement mensuel du parrain
- **Plafond** : 20 filleuls = **100% gratuit** (abonnement offert à vie)
- **Validation** : Un filleul compte **SEULEMENT s'il paie son abonnement chaque mois**
- **Mise à jour mensuelle automatique** : Recalcul le 1er de chaque mois

### Exemples Concrets
| Filleuls actifs | Réduction | Prix mensuel | Économie |
|-----------------|-----------|--------------|----------|
| 0 filleul       | 0%        | 30,00 €      | 0 €      |
| 1 filleul       | -5%       | 28,50 €      | 1,50 €   |
| 5 filleuls      | -25%      | 22,50 €      | 7,50 €   |
| 10 filleuls     | -50%      | 15,00 €      | 15,00 €  |
| 15 filleuls     | -75%      | 7,50 €       | 22,50 €  |
| 20 filleuls     | -100%     | **GRATUIT**  | 30,00 €  |

---

## 🎁 Bonus de Bienvenue (Optionnel)

### Pour le Filleul
- **1er mois à -30%** (21€ au lieu de 30€)
- OU **Essai gratuit 14 jours** sans CB

### Pour le Parrain
- **Prime ponctuelle de 10€** à la première souscription payante du filleul
- Versée le mois suivant l'inscription

---

## 📊 Interface de Suivi (Modal dans Options)

### Affichage Principal
```
┌─────────────────────────────────────────┐
│   MON PROGRAMME DE PARRAINAGE           │
├─────────────────────────────────────────┤
│                                         │
│  Votre réduction actuelle : -45%        │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  9/20 filleuls actifs                   │
│                                         │
│  Votre abonnement : 16,50€/mois         │
│  (au lieu de 30€)                       │
│                                         │
│  ➡️ Plus que 11 filleuls pour           │
│     l'abonnement GRATUIT !              │
└─────────────────────────────────────────┘
```

### Section Lien de Parrainage
```
┌─────────────────────────────────────────┐
│  🔗 VOTRE LIEN DE PARRAINAGE            │
│                                         │
│  liveownerunit.com/signup?ref=ABC123    │
│  [📋 Copier]  [📧 Email]  [📱 WhatsApp] │
│                                         │
│  [QR CODE généré automatiquement]       │
└─────────────────────────────────────────┘
```

### Statistiques
```
┌─────────────────────────────────────────┐
│  📊 VOS STATISTIQUES                    │
│                                         │
│  • Invitations envoyées : 24            │
│  • Inscriptions : 12 (50%)              │
│  • Filleuls actifs payants : 9          │
│  • Filleuls inactifs : 3                │
│  • Économies totales : 121,50€          │
└─────────────────────────────────────────┘
```

### Liste des Filleuls
```
┌──────────────────────────────────────┐
│ ✅ Jean D. • Actif • -5%             │
│    Inscrit le 15/01/2026             │
│    Dernier paiement : 01/02/2026     │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│ ✅ Marie L. • Active • -5%           │
│    Inscrite le 03/02/2026            │
│    Dernier paiement : 01/02/2026     │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│ ❌ Paul M. • Inactif • 0%            │
│    Inscrit le 10/12/2025             │
│    Abonnement résilié                │
└──────────────────────────────────────┘
```

---

## 📝 PROMPT COPILOT COMPLET

```
Crée un système de parrainage professionnel pour LiveOwnerUnit (SaaS gestion gîtes) avec réductions progressives basées sur filleuls actifs payants.

═══════════════════════════════════════════════════════════════════
RÈGLES MÉTIER CRITIQUES
═══════════════════════════════════════════════════════════════════

1. CALCUL DE RÉDUCTION :
   - 1 filleul actif payant = -5% de réduction
   - Maximum 20 filleuls = -100% (gratuit)
   - Un filleul compte SEULEMENT s'il paie son abonnement chaque mois
   
2. MISE À JOUR MENSUELLE :
   - Recalcul automatique le 1er de chaque mois
   - Vérification du statut de paiement de chaque filleul
   - Si un filleul arrête de payer → il ne compte plus
   - Si un filleul recommence à payer → il compte à nouveau
   
3. PRIX DE BASE :
   - Abonnement standard : 30€/mois

═══════════════════════════════════════════════════════════════════
ARCHITECTURE BASE DE DONNÉES (Supabase PostgreSQL)
═══════════════════════════════════════════════════════════════════

Table 1: referrals
──────────────────────────────────────────────────────────────────
CREATE TABLE referrals (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  referrer_id UUID REFERENCES users(id) ON DELETE CASCADE,
  referral_code VARCHAR(12) UNIQUE NOT NULL,
  referred_email VARCHAR(255),
  referred_user_id UUID REFERENCES users(id) ON DELETE SET NULL,
  status VARCHAR(20) DEFAULT 'pending', 
    -- Valeurs possibles : 'pending', 'registered', 'active', 'inactive'
  registered_at TIMESTAMP,
  first_payment_at TIMESTAMP,
  last_payment_at TIMESTAMP,
  is_currently_paying BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_referrals_referrer ON referrals(referrer_id);
CREATE INDEX idx_referrals_code ON referrals(referral_code);


Table 2: referral_rewards
──────────────────────────────────────────────────────────────────
CREATE TABLE referral_rewards (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  month VARCHAR(7), -- Format 'YYYY-MM' ex: '2026-02'
  active_referrals_count INT DEFAULT 0,
  discount_percentage INT DEFAULT 0, -- 0 à 100
  original_price DECIMAL(10,2) DEFAULT 30.00,
  discounted_price DECIMAL(10,2),
  total_saved DECIMAL(10,2),
  created_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(user_id, month)
);

CREATE INDEX idx_rewards_user_month ON referral_rewards(user_id, month);


Table 3: referral_invitations (tracking optionnel)
──────────────────────────────────────────────────────────────────
CREATE TABLE referral_invitations (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  referrer_id UUID REFERENCES users(id) ON DELETE CASCADE,
  channel VARCHAR(50), -- 'email', 'whatsapp', 'linkedin', 'copy'
  sent_at TIMESTAMP DEFAULT NOW()
);


Fonction PostgreSQL: Calcul de la réduction
──────────────────────────────────────────────────────────────────
CREATE OR REPLACE FUNCTION calculate_monthly_referral_discount(p_user_id UUID)
RETURNS TABLE (
  active_count INT,
  discount_pct INT,
  new_price DECIMAL
) AS $$
DECLARE
  v_active_count INT;
  v_discount INT;
  v_base_price DECIMAL := 30.00;
BEGIN
  -- Compter les filleuls actifs payants
  SELECT COUNT(*) INTO v_active_count
  FROM referrals
  WHERE referrer_id = p_user_id
    AND status = 'active'
    AND is_currently_paying = true;
  
  -- Calcul de la réduction (plafond 100%)
  v_discount := LEAST(v_active_count * 5, 100);
  
  RETURN QUERY
  SELECT 
    v_active_count,
    v_discount,
    ROUND(v_base_price * (100 - v_discount) / 100, 2);
END;
$$ LANGUAGE plpgsql;


═══════════════════════════════════════════════════════════════════
INTERFACE UTILISATEUR (MODAL)
═══════════════════════════════════════════════════════════════════

HTML Structure
──────────────────────────────────────────────────────────────────
<div id="referralModal" class="modal" style="display: none;">
  <div class="modal-content referral-modal">
    <div class="modal-header">
      <h2>
        <i data-lucide="users"></i>
        Mon Programme de Parrainage
      </h2>
      <button onclick="closeReferralModal()" class="close-btn">
        <i data-lucide="x"></i>
      </button>
    </div>
    
    <div class="modal-body">
      
      <!-- Section 1: Réduction actuelle -->
      <div class="referral-status-card">
        <div class="discount-display">
          <div class="discount-badge" id="currentDiscountBadge">-0%</div>
          <div class="discount-details">
            <p class="discount-label">Votre réduction actuelle</p>
            <p class="discount-price">
              <span class="original-price">30€</span>
              <span class="new-price" id="currentPrice">30€</span>
              <span class="period">/mois</span>
            </p>
          </div>
        </div>
        
        <div class="progress-container">
          <div class="progress-bar">
            <div class="progress-fill" id="progressFill" style="width: 0%"></div>
          </div>
          <div class="progress-label">
            <span id="activeCount">0</span>/20 filleuls actifs
          </div>
        </div>
        
        <div class="next-milestone" id="nextMilestone">
          ➡️ Plus que <strong>20</strong> filleuls pour l'abonnement GRATUIT !
        </div>
      </div>
      
      <!-- Section 2: Lien de parrainage -->
      <div class="referral-link-section">
        <h3>🔗 Votre lien de parrainage</h3>
        <div class="link-container">
          <input 
            type="text" 
            readonly 
            id="referralLinkInput" 
            value="https://liveownerunit.com/signup?ref=ABC123"
          >
          <button onclick="copyReferralLink()" class="copy-btn" title="Copier">
            <i data-lucide="copy"></i>
          </button>
        </div>
        
        <div class="share-buttons">
          <button onclick="shareViaEmail()" class="share-btn email-btn">
            <i data-lucide="mail"></i>
            Email
          </button>
          <button onclick="shareViaWhatsApp()" class="share-btn whatsapp-btn">
            <i data-lucide="message-circle"></i>
            WhatsApp
          </button>
          <button onclick="shareViaLinkedIn()" class="share-btn linkedin-btn">
            <i data-lucide="linkedin"></i>
            LinkedIn
          </button>
        </div>
        
        <div class="qr-code-container">
          <p class="qr-label">QR Code pour partage physique</p>
          <canvas id="qrCodeCanvas" width="200" height="200"></canvas>
          <button onclick="downloadQRCode()" class="download-qr-btn">
            <i data-lucide="download"></i>
            Télécharger le QR Code
          </button>
        </div>
      </div>
      
      <!-- Section 3: Statistiques -->
      <div class="referral-stats-section">
        <h3>📊 Vos statistiques</h3>
        <div class="stats-grid">
          <div class="stat-card">
            <div class="stat-icon"><i data-lucide="send"></i></div>
            <div class="stat-content">
              <span class="stat-value" id="invitationsSent">0</span>
              <span class="stat-label">Invitations envoyées</span>
            </div>
          </div>
          
          <div class="stat-card">
            <div class="stat-icon"><i data-lucide="user-check"></i></div>
            <div class="stat-content">
              <span class="stat-value" id="registrations">0</span>
              <span class="stat-label">Inscriptions</span>
            </div>
          </div>
          
          <div class="stat-card active">
            <div class="stat-icon"><i data-lucide="user-plus"></i></div>
            <div class="stat-content">
              <span class="stat-value" id="activeReferrals">0</span>
              <span class="stat-label">Filleuls actifs</span>
            </div>
          </div>
          
          <div class="stat-card">
            <div class="stat-icon"><i data-lucide="piggy-bank"></i></div>
            <div class="stat-content">
              <span class="stat-value" id="totalSaved">0€</span>
              <span class="stat-label">Économies totales</span>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Section 4: Liste des filleuls -->
      <div class="referrals-list-section">
        <h3>👥 Mes filleuls</h3>
        <div id="referralsList" class="referrals-list">
          <!-- Généré dynamiquement -->
        </div>
      </div>
      
      <!-- Section 5: Conseils -->
      <div class="referral-tips-section">
        <h3>💡 Comment parrainer efficacement ?</h3>
        <ul class="tips-list">
          <li>
            <i data-lucide="check-circle"></i>
            Partagez votre expérience sur les groupes Facebook de propriétaires
          </li>
          <li>
            <i data-lucide="check-circle"></i>
            Montrez vos gains de temps concrets avec LiveOwnerUnit
          </li>
          <li>
            <i data-lucide="check-circle"></i>
            Invitez vos collègues lors de réunions Gîtes de France
          </li>
          <li>
            <i data-lucide="check-circle"></i>
            Publiez un avis détaillé sur les forums spécialisés
          </li>
        </ul>
      </div>
      
    </div>
  </div>
</div>


═══════════════════════════════════════════════════════════════════
CODE JAVASCRIPT
═══════════════════════════════════════════════════════════════════

1. GÉNÉRATION DU CODE DE PARRAINAGE
──────────────────────────────────────────────────────────────────
async function generateReferralCode() {
  const chars = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789';
  let code = '';
  
  for (let i = 0; i < 8; i++) {
    code += chars.charAt(Math.floor(Math.random() * chars.length));
  }
  
  // Vérifier l'unicité en base
  const { data } = await supabase
    .from('referrals')
    .select('referral_code')
    .eq('referral_code', code)
    .single();
  
  if (data) {
    return generateReferralCode(); // Régénérer si existe
  }
  
  return code;
}


2. CHARGEMENT DES DONNÉES DE PARRAINAGE
──────────────────────────────────────────────────────────────────
async function loadReferralData() {
  const userId = supabase.auth.user().id;
  
  // 1. Récupérer ou créer le code de parrainage
  let { data: existingReferral } = await supabase
    .from('referrals')
    .select('referral_code')
    .eq('referrer_id', userId)
    .limit(1)
    .single();
  
  let referralCode;
  
  if (!existingReferral) {
    referralCode = await generateReferralCode();
    
    await supabase.from('referrals').insert({
      referrer_id: userId,
      referral_code: referralCode,
      status: 'pending'
    });
  } else {
    referralCode = existingReferral.referral_code;
  }
  
  // 2. Afficher le lien
  const referralLink = `https://liveownerunit.com/signup?ref=${referralCode}`;
  document.getElementById('referralLinkInput').value = referralLink;
  
  // 3. Générer le QR Code
  generateQRCode(referralLink);
  
  // 4. Calculer la réduction actuelle
  const { data: discountData } = await supabase
    .rpc('calculate_monthly_referral_discount', {
      p_user_id: userId
    });
  
  if (discountData && discountData.length > 0) {
    const { active_count, discount_pct, new_price } = discountData[0];
    
    // Mettre à jour l'affichage
    document.getElementById('currentDiscountBadge').textContent = `-${discount_pct}%`;
    document.getElementById('currentPrice').textContent = `${new_price}€`;
    document.getElementById('activeCount').textContent = active_count;
    document.getElementById('progressFill').style.width = `${(active_count / 20) * 100}%`;
    
    // Message de progression
    const remaining = 20 - active_count;
    if (remaining > 0) {
      document.getElementById('nextMilestone').innerHTML = 
        `➡️ Plus que <strong>${remaining}</strong> filleul${remaining > 1 ? 's' : ''} pour l'abonnement GRATUIT !`;
    } else {
      document.getElementById('nextMilestone').innerHTML = 
        `🎉 <strong>Félicitations !</strong> Votre abonnement est GRATUIT à vie !`;
    }
  }
  
  // 5. Charger les statistiques
  await loadReferralStats(userId);
  
  // 6. Charger la liste des filleuls
  await loadReferralsList(userId);
}


3. CHARGEMENT DES STATISTIQUES
──────────────────────────────────────────────────────────────────
async function loadReferralStats(userId) {
  
  // Invitations envoyées
  const { data: invitations } = await supabase
    .from('referral_invitations')
    .select('id')
    .eq('referrer_id', userId);
  
  document.getElementById('invitationsSent').textContent = invitations?.length || 0;
  
  // Inscriptions totales (status != 'pending')
  const { data: registrations } = await supabase
    .from('referrals')
    .select('id')
    .eq('referrer_id', userId)
    .neq('status', 'pending');
  
  document.getElementById('registrations').textContent = registrations?.length || 0;
  
  // Filleuls actifs payants
  const { data: activeRefs } = await supabase
    .from('referrals')
    .select('id')
    .eq('referrer_id', userId)
    .eq('status', 'active')
    .eq('is_currently_paying', true);
  
  document.getElementById('activeReferrals').textContent = activeRefs?.length || 0;
  
  // Économies totales cumulées
  const { data: rewards } = await supabase
    .from('referral_rewards')
    .select('total_saved')
    .eq('user_id', userId);
  
  const totalSaved = rewards?.reduce((sum, r) => sum + parseFloat(r.total_saved || 0), 0) || 0;
  document.getElementById('totalSaved').textContent = `${totalSaved.toFixed(2)}€`;
}


4. CHARGEMENT DE LA LISTE DES FILLEULS
──────────────────────────────────────────────────────────────────
async function loadReferralsList(userId) {
  const { data: referrals } = await supabase
    .from('referrals')
    .select(`
      id,
      referred_email,
      status,
      registered_at,
      last_payment_at,
      is_currently_paying,
      users:referred_user_id (
        first_name,
        last_name
      )
    `)
    .eq('referrer_id', userId)
    .neq('status', 'pending')
    .order('registered_at', { ascending: false });
  
  const listContainer = document.getElementById('referralsList');
  listContainer.innerHTML = '';
  
  if (!referrals || referrals.length === 0) {
    listContainer.innerHTML = `
      <div class="no-referrals">
        <i data-lucide="users" style="width: 48px; height: 48px; opacity: 0.3;"></i>
        <p>Vous n'avez pas encore de filleuls</p>
        <p class="subtitle">Partagez votre lien pour commencer !</p>
      </div>
    `;
    lucide.createIcons();
    return;
  }
  
  referrals.forEach(ref => {
    const isActive = ref.status === 'active' && ref.is_currently_paying;
    
    const name = ref.users 
      ? `${ref.users.first_name} ${ref.users.last_name.charAt(0)}.`
      : ref.referred_email.split('@')[0];
    
    const card = document.createElement('div');
    card.className = `referral-item ${isActive ? 'active' : 'inactive'}`;
    
    card.innerHTML = `
      <div class="referral-item-status">
        ${isActive ? '✅' : '❌'}
      </div>
      <div class="referral-item-content">
        <div class="referral-item-header">
          <strong class="referral-name">${name}</strong>
          <span class="referral-badge ${isActive ? 'badge-active' : 'badge-inactive'}">
            ${isActive ? 'Actif' : 'Inactif'}
          </span>
        </div>
        <div class="referral-item-details">
          <small class="detail-item">
            <i data-lucide="calendar"></i>
            Inscrit le ${new Date(ref.registered_at).toLocaleDateString('fr-FR')}
          </small>
          ${ref.last_payment_at ? `
            <small class="detail-item">
              <i data-lucide="credit-card"></i>
              Dernier paiement : ${new Date(ref.last_payment_at).toLocaleDateString('fr-FR')}
            </small>
          ` : ''}
        </div>
      </div>
      <div class="referral-item-discount">
        <span class="discount-value ${isActive ? 'active' : ''}">
          ${isActive ? '-5%' : '0%'}
        </span>
      </div>
    `;
    
    listContainer.appendChild(card);
  });
  
  lucide.createIcons();
}


5. COPIER LE LIEN DE PARRAINAGE
──────────────────────────────────────────────────────────────────
function copyReferralLink() {
  const input = document.getElementById('referralLinkInput');
  input.select();
  input.setSelectionRange(0, 99999); // Mobile
  
  document.execCommand('copy');
  
  showToast('Lien copié dans le presse-papier !', 'success');
  
  // Animation du bouton
  const btn = event.target.closest('.copy-btn');
  btn.innerHTML = '<i data-lucide="check"></i>';
  lucide.createIcons();
  
  setTimeout(() => {
    btn.innerHTML = '<i data-lucide="copy"></i>';
    lucide.createIcons();
  }, 2000);
}


6. PARTAGE PAR EMAIL
──────────────────────────────────────────────────────────────────
function shareViaEmail() {
  const link = document.getElementById('referralLinkInput').value;
  
  const subject = encodeURIComponent('Rejoignez LiveOwnerUnit - Gestion de gîtes simplifiée');
  const body = encodeURIComponent(
`Bonjour,

Je vous recommande LiveOwnerUnit, une plateforme professionnelle pour gérer vos locations saisonnières (gîtes, meublés de tourisme).

Fonctionnalités principales :
- Synchronisation automatique Airbnb, Booking, Gîtes de France
- Gestion fiscale LMNP/LMP avec simulations
- Fiches clients interactives
- Planning ménage et maintenance

Inscrivez-vous via ce lien pour bénéficier d'une réduction de bienvenue :
${link}

Cordialement`
  );
  
  window.location.href = `mailto:?subject=${subject}&body=${body}`;
  
  // Tracker l'invitation
  trackInvitation('email');
}


7. PARTAGE PAR WHATSAPP
──────────────────────────────────────────────────────────────────
function shareViaWhatsApp() {
  const link = document.getElementById('referralLinkInput').value;
  
  const text = encodeURIComponent(
`Salut ! 👋

Je te recommande LiveOwnerUnit pour gérer tes gîtes.

C'est une super plateforme qui m'a fait gagner un temps fou : synchro automatique des réservations, gestion fiscale, fiches clients...

Inscris-toi via ce lien pour profiter d'une réduction :
${link}`
  );
  
  window.open(`https://wa.me/?text=${text}`, '_blank');
  
  trackInvitation('whatsapp');
}


8. PARTAGE PAR LINKEDIN
──────────────────────────────────────────────────────────────────
function shareViaLinkedIn() {
  const link = document.getElementById('referralLinkInput').value;
  
  const url = encodeURIComponent(link);
  const title = encodeURIComponent('LiveOwnerUnit - Gestion de locations saisonnières');
  const summary = encodeURIComponent('Plateforme complète pour propriétaires de gîtes');
  
  window.open(
    `https://www.linkedin.com/sharing/share-offsite/?url=${url}`,
    '_blank'
  );
  
  trackInvitation('linkedin');
}


9. GÉNÉRER LE QR CODE
──────────────────────────────────────────────────────────────────
function generateQRCode(link) {
  const canvas = document.getElementById('qrCodeCanvas');
  
  // Utiliser la librairie qrcode.js
  QRCode.toCanvas(canvas, link, {
    width: 200,
    margin: 2,
    color: {
      dark: '#000000',
      light: '#FFFFFF'
    }
  }, (error) => {
    if (error) console.error('Erreur QR Code:', error);
  });
}


10. TÉLÉCHARGER LE QR CODE
──────────────────────────────────────────────────────────────────
function downloadQRCode() {
  const canvas = document.getElementById('qrCodeCanvas');
  const link = document.createElement('a');
  link.download = 'qr-code-parrainage-liveownerunit.png';
  link.href = canvas.toDataURL('image/png');
  link.click();
  
  showToast('QR Code téléchargé !', 'success');
}


11. TRACKER UNE INVITATION
──────────────────────────────────────────────────────────────────
async function trackInvitation(channel) {
  const userId = supabase.auth.user().id;
  
  await supabase.from('referral_invitations').insert({
    referrer_id: userId,
    channel: channel,
    sent_at: new Date().toISOString()
  });
  
  // Recharger les stats
  await loadReferralStats(userId);
}


12. PROCESSUS D'INSCRIPTION AVEC CODE PARRAIN
──────────────────────────────────────────────────────────────────
// Sur la page signup.html

window.addEventListener('DOMContentLoaded', () => {
  checkReferralCode();
});

function checkReferralCode() {
  const urlParams = new URLSearchParams(window.location.search);
  const refCode = urlParams.get('ref');
  
  if (refCode) {
    // Afficher un badge de bienvenue
    document.getElementById('referralBanner').style.display = 'block';
    document.getElementById('referralBanner').innerHTML = `
      <div class="referral-welcome">
        <i data-lucide="gift"></i>
        <div>
          <strong>Vous êtes invité(e) par un membre !</strong>
          <p>Bénéficiez de 30% de réduction sur votre premier mois</p>
        </div>
      </div>
    `;
    
    // Stocker le code pour l'utiliser après inscription
    sessionStorage.setItem('referralCode', refCode);
    
    lucide.createIcons();
  }
}

async function handleSignup(formData) {
  const { email, password, firstName, lastName } = formData;
  
  // Créer le compte
  const { user, error } = await supabase.auth.signUp({
    email: email,
    password: password,
    options: {
      data: {
        first_name: firstName,
        last_name: lastName
      }
    }
  });
  
  if (error) {
    showToast('Erreur lors de l\'inscription', 'error');
    return;
  }
  
  // Vérifier si code de parrainage présent
  const referralCode = sessionStorage.getItem('referralCode');
  
  if (referralCode) {
    // Vérifier la validité du code
    const { data: referral } = await supabase
      .from('referrals')
      .select('referrer_id, referral_code')
      .eq('referral_code', referralCode)
      .single();
    
    if (referral) {
      // Créer l'entrée de parrainage
      await supabase.from('referrals').insert({
        referrer_id: referral.referrer_id,
        referral_code: referralCode,
        referred_email: email,
        referred_user_id: user.id,
        status: 'registered',
        registered_at: new Date().toISOString()
      });
      
      // Appliquer le bonus 30% sur le premier mois
      await supabase.from('user_subscriptions').insert({
        user_id: user.id,
        first_month_discount: 30,
        applied_at: new Date().toISOString()
      });
      
      // Nettoyer le sessionStorage
      sessionStorage.removeItem('referralCode');
      
      showToast('Inscription réussie ! Réduction de 30% appliquée', 'success');
    }
  }
  
  // Rediriger vers dashboard
  window.location.href = '/index.html';
}


13. MISE À JOUR MENSUELLE AUTOMATIQUE (Edge Function Supabase)
──────────────────────────────────────────────────────────────────
// Fichier: supabase/functions/update-monthly-referrals/index.ts
// À exécuter via Cron Job le 1er de chaque mois à 00:00

import { createClient } from '@supabase/supabase-js'

Deno.serve(async (req) => {
  const supabase = createClient(
    Deno.env.get('SUPABASE_URL')!,
    Deno.env.get('SUPABASE_SERVICE_KEY')!
  )
  
  console.log('Début de la mise à jour mensuelle des parrainages')
  
  // Récupérer tous les utilisateurs ayant des filleuls
  const { data: referrers } = await supabase
    .from('referrals')
    .select('referrer_id')
    .not('referrer_id', 'is', null)
  
  const uniqueReferrers = [...new Set(referrers.map(r => r.referrer_id))]
  
  console.log(`${uniqueReferrers.length} parrains à traiter`)
  
  for (const referrerId of uniqueReferrers) {
    
    // Récupérer tous les filleuls du parrain
    const { data: referrals } = await supabase
      .from('referrals')
      .select('id, referred_user_id, status')
      .eq('referrer_id', referrerId)
      .neq('status', 'pending')
    
    let activePayingCount = 0
    
    for (const ref of referrals) {
      // Vérifier si le filleul a payé ce mois-ci
      const firstOfMonth = new Date()
      firstOfMonth.setDate(1)
      firstOfMonth.setHours(0, 0, 0, 0)
      
      const { data: payment } = await supabase
        .from('payments')
        .select('id, amount, created_at')
        .eq('user_id', ref.referred_user_id)
        .gte('created_at', firstOfMonth.toISOString())
        .eq('status', 'succeeded')
        .single()
      
      if (payment) {
        // Filleul actif et payant
        activePayingCount++
        
        await supabase
          .from('referrals')
          .update({
            status: 'active',
            is_currently_paying: true,
            last_payment_at: payment.created_at
          })
          .eq('id', ref.id)
        
      } else {
        // Filleul inactif (n'a pas payé ce mois)
        await supabase
          .from('referrals')
          .update({
            status: ref.status === 'active' ? 'inactive' : ref.status,
            is_currently_paying: false
          })
          .eq('id', ref.id)
      }
    }
    
    // Calculer la réduction
    const discountPct = Math.min(activePayingCount * 5, 100)
    const basePrice = 30.00
    const discountedPrice = basePrice * (100 - discountPct) / 100
    const saved = basePrice - discountedPrice
    
    // Enregistrer la récompense du mois
    const currentMonth = new Date().toISOString().slice(0, 7) // YYYY-MM
    
    await supabase
      .from('referral_rewards')
      .upsert({
        user_id: referrerId,
        month: currentMonth,
        active_referrals_count: activePayingCount,
        discount_percentage: discountPct,
        original_price: basePrice,
        discounted_price: discountedPrice,
        total_saved: saved
      }, {
        onConflict: 'user_id,month'
      })
    
    // Envoyer notification au parrain
    await supabase.from('notifications').insert({
      user_id: referrerId,
      type: 'referral_monthly_update',
      title: 'Mise à jour de votre programme de parrainage',
      message: activePayingCount === 0
        ? `Aucun filleul actif ce mois-ci. Votre abonnement : ${basePrice}€/mois`
        : `Vous avez ${activePayingCount} filleul${activePayingCount > 1 ? 's' : ''} actif${activePayingCount > 1 ? 's' : ''}. Votre réduction : -${discountPct}% (${discountedPrice}€/mois au lieu de ${basePrice}€)`,
      created_at: new Date().toISOString()
    })
    
    console.log(`Parrain ${referrerId}: ${activePayingCount} actifs, -${discountPct}%`)
  }
  
  return new Response(
    JSON.stringify({ 
      success: true, 
      processed: uniqueReferrers.length 
    }),
    { headers: { 'Content-Type': 'application/json' } }
  )
})

// Configuration du Cron (dans Supabase Dashboard)
// Schedule: 0 0 1 * * (tous les 1er du mois à minuit)


═══════════════════════════════════════════════════════════════════
CSS - STYLES
═══════════════════════════════════════════════════════════════════

.referral-modal .modal-content {
  max-width: 700px;
  max-height: 90vh;
  overflow-y: auto;
}

.referral-status-card {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  padding: 2rem;
  border-radius: 16px;
  margin-bottom: 2rem;
}

.discount-display {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  margin-bottom: 1.5rem;
}

.discount-badge {
  font-size: 3rem;
  font-weight: bold;
  background: rgba(255, 255, 255, 0.2);
  backdrop-filter: blur(10px);
  padding: 1rem 2rem;
  border-radius: 20px;
  min-width: 120px;
  text-align: center;
}

.discount-details {
  flex: 1;
}

.discount-label {
  font-size: 0.875rem;
  opacity: 0.9;
  margin: 0 0 0.5rem 0;
}

.discount-price {
  font-size: 1.75rem;
  font-weight: bold;
  margin: 0;
}

.original-price {
  text-decoration: line-through;
  opacity: 0.6;
  margin-right: 0.5rem;
  font-size: 1.25rem;
}

.period {
  font-size: 1rem;
  opacity: 0.8;
}

.progress-container {
  margin-bottom: 1rem;
}

.progress-bar {
  width: 100%;
  height: 12px;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 10px;
  overflow: hidden;
  margin-bottom: 0.5rem;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #10b981, #34d399);
  border-radius: 10px;
  transition: width 0.5s ease;
}

.progress-label {
  font-size: 0.875rem;
  text-align: center;
  opacity: 0.9;
}

.next-milestone {
  background: rgba(255, 255, 255, 0.15);
  padding: 1rem;
  border-radius: 8px;
  text-align: center;
  font-size: 0.938rem;
}

.referral-link-section {
  background: #f9fafb;
  padding: 1.5rem;
  border-radius: 12px;
  margin-bottom: 2rem;
}

.link-container {
  display: flex;
  gap: 0.5rem;
  margin: 1rem 0;
}

.link-container input {
  flex: 1;
  padding: 0.75rem 1rem;
  border: 2px solid #e5e7eb;
  border-radius: 8px;
  font-size: 0.875rem;
  background: white;
}

.copy-btn {
  padding: 0.75rem 1rem;
  background: #667eea;
  color: white;
  border: none;
  border-radius: 8px;
  cursor: pointer;
  transition: background 0.2s;
}

.copy-btn:hover {
  background: #5568d3;
}

.share-buttons {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0.75rem;
  margin: 1.5rem 0;
}

.share-btn {
  padding: 0.75rem 1rem;
  border: 2px solid #e5e7eb;
  background: white;
  border-radius: 8px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  font-size: 0.875rem;
  font-weight: 500;
  transition: all 0.2s;
}

.share-btn:hover {
  border-color: #667eea;
  background: #f0f4ff;
}

.qr-code-container {
  text-align: center;
  padding: 1.5rem;
  background: white;
  border-radius: 8px;
}

.qr-label {
  font-size: 0.875rem;
  color: #6b7280;
  margin-bottom: 1rem;
}

#qrCodeCanvas {
  margin: 0 auto 1rem auto;
  display: block;
}

.download-qr-btn {
  padding: 0.5rem 1rem;
  background: #f3f4f6;
  border: 1px solid #e5e7eb;
  border-radius: 6px;
  cursor: pointer;
  font-size: 0.875rem;
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
}

.stats-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 1rem;
  margin-top: 1rem;
}

.stat-card {
  background: #f9fafb;
  padding: 1.5rem;
  border-radius: 12px;
  display: flex;
  align-items: center;
  gap: 1rem;
  border: 2px solid transparent;
  transition: all 0.2s;
}

.stat-card.active {
  background: #ecfdf5;
  border-color: #10b981;
}

.stat-icon {
  width: 48px;
  height: 48px;
  background: white;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #667eea;
}

.stat-content {
  flex: 1;
}

.stat-value {
  display: block;
  font-size: 1.75rem;
  font-weight: bold;
  color: #111827;
  line-height: 1;
  margin-bottom: 0.25rem;
}

.stat-label {
  display: block;
  font-size: 0.813rem;
  color: #6b7280;
}

.referrals-list {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
  margin-top: 1rem;
}

.referral-item {
  display: flex;
  align-items: center;
  gap: 1rem;
  padding: 1rem;
  background: #f9fafb;
  border: 2px solid #e5e7eb;
  border-radius: 12px;
  transition: all 0.2s;
}

.referral-item.active {
  background: #ecfdf5;
  border-color: #10b981;
}

.referral-item.inactive {
  opacity: 0.6;
}

.referral-item-status {
  font-size: 1.5rem;
}

.referral-item-content {
  flex: 1;
}

.referral-item-header {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  margin-bottom: 0.5rem;
}

.referral-name {
  font-size: 1rem;
  color: #111827;
}

.referral-badge {
  font-size: 0.75rem;
  padding: 0.25rem 0.75rem;
  border-radius: 12px;
  font-weight: 600;
}

.badge-active {
  background: #d1fae5;
  color: #065f46;
}

.badge-inactive {
  background: #fee2e2;
  color: #991b1b;
}

.referral-item-details {
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
}

.detail-item {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  font-size: 0.813rem;
  color: #6b7280;
}

.referral-item-discount {
  text-align: center;
}

.discount-value {
  display: inline-block;
  font-size: 1.25rem;
  font-weight: bold;
  padding: 0.5rem 1rem;
  border-radius: 8px;
  background: #e5e7eb;
  color: #6b7280;
}

.discount-value.active {
  background: #10b981;
  color: white;
}

.no-referrals {
  text-align: center;
  padding: 3rem 2rem;
  color: #6b7280;
}

.no-referrals p {
  margin: 0.5rem 0;
}

.subtitle {
  font-size: 0.875rem;
  opacity: 0.8;
}

.tips-list {
  list-style: none;
  padding: 0;
  margin: 1rem 0 0 0;
}

.tips-list li {
  display: flex;
  align-items: flex-start;
  gap: 0.75rem;
  padding: 0.75rem 0;
  font-size: 0.938rem;
  color: #374151;
}

.tips-list li svg {
  color: #10b981;
  flex-shrink: 0;
  margin-top: 0.125rem;
}

@media (max-width: 640px) {
  .stats-grid {
    grid-template-columns: 1fr;
  }
  
  .discount-display {
    flex-direction: column;
    text-align: center;
  }
  
  .share-buttons {
    grid-template-columns: 1fr;
  }
}


═══════════════════════════════════════════════════════════════════
OUVERTURE DU MODAL (depuis Options)
═══════════════════════════════════════════════════════════════════

// Dans la page Options, ajouter un bouton
<div class="option-item" onclick="openReferralModal()">
  <div class="option-icon">
    <i data-lucide="users"></i>
  </div>
  <div class="option-content">
    <h4>Programme de Parrainage</h4>
    <p>Invitez vos collègues et réduisez votre abonnement</p>
  </div>
  <div class="option-action">
    <i data-lucide="chevron-right"></i>
  </div>
</div>

async function openReferralModal() {
  document.getElementById('referralModal').style.display = 'flex';
  await loadReferralData();
}

function closeReferralModal() {
  document.getElementById('referralModal').style.display = 'none';
}


═══════════════════════════════════════════════════════════════════
LIBRAIRIES REQUISES
═══════════════════════════════════════════════════════════════════

<!-- QR Code Generator -->
<script src="https://cdn.jsdelivr.net/npm/qrcode@1.5.3/build/qrcode.min.js"></script>

<!-- Lucide Icons (déjà présent) -->
<script src="https://unpkg.com/lucide@latest"></script>


═══════════════════════════════════════════════════════════════════
POINTS IMPORTANTS
═══════════════════════════════════════════════════════════════════

1. SÉCURITÉ :
   - Code de parrainage unique et aléatoire (8 caractères)
   - Vérification anti-fraude potentielle (même IP, même CB)
   - RLS sur toutes les tables

2. PERFORMANCE :
   - Mise à jour mensuelle automatique via Edge Function
   - Calcul en temps réel avec fonction PostgreSQL
   - Cache des statistiques pour éviter recalculs multiples

3. TRANSPARENCE :
   - L'utilisateur voit toujours son nombre exact de filleuls actifs
   - Historique mensuel des économies réalisées
   - Notifications lors des changements de statut

4. EXPÉRIENCE UTILISATEUR :
   - Interface claire et motivante
   - Partage facilité (email, WhatsApp, LinkedIn, QR Code)
   - Progression visuelle vers l'objectif gratuit

Cette implémentation crée un système de parrainage transparent, motivant et totalement automatisé.
```


## 📊 4. TABLEAUX DE BORD INTERACTIFS & INSIGHTS IA

### Concept
Analyse intelligente des données avec recommandations actionnables.

### Bénéfices
- **Revenus +15-30%** grâce à l'optimisation
- Décisions basées sur les données vs intuition
- Anticipation des tendances

### Fonctionnalités clés
- **Prédictions IA** :
  - "Votre chiffre d'affaires prévu pour août : 4 200€ ±300€"
  - "Probabilité de remplissage à 90% pour le week-end du 14 juillet"
  - "Meilleur moment pour baisser vos prix : dans 3 semaines"
- **Recommandations personnalisées** :
  - "Vos concurrents baissent leurs prix de 15%. Action recommandée : -10%"
  - "Votre gîte des Roses a un taux d'annulation élevé. Cause probable : photos obsolètes"
  - "Vous perdez 800€/mois en commissions. Stratégie suggérée : +30% de réservations directes"
- **Benchmarking** :
  - Comparaison avec gîtes similaires de la région
  - Positionnement prix vs concurrence
  - Part de marché locale
- **Alertes intelligentes** :
  - "Concurrent à 5km vient de baisser ses prix de 20%"
  - "Événement local dans 2 mois : augmentez vos tarifs"
  - "Votre taux de réponse < moyenne : risque de perte de visibilité Airbnb"

### 📝 Prompt Copilot
```
Crée un système d'analyse IA pour LiveOwnerUnit avec recommandations intelligentes.

Composants :

1. Widget "Insights IA" sur le dashboard :
   - Analyse automatique des données utilisateur
   - 3-5 recommandations personnalisées affichées
   - Niveau de priorité : Urgent / Important / Suggestion
   - Bouton "Appliquer" pour action en 1 clic

2. Types d'analyses à faire :
   - Prédiction CA (régression linéaire sur historique)
   - Détection anomalies (taux annulation, temps réponse)
   - Comparaison concurrence (si données disponibles)
   - Optimisation calendrier (jours avec faible TO)

3. Recommandations types à générer :
   - "Votre taux d'occupation pour [mois] est à [X]%. Recommandation : [action]"
   - "Vos prix sont [X]% au-dessus de la moyenne locale. Impact estimé : [Y] réservations perdues"
   - "Vous n'avez pas envoyé de fiche client pour 3 réservations. Envoyez-les maintenant ?"

4. Graphiques interactifs :
   - Évolution CA vs prévisions
   - Taux occupation réel vs objectif
   - Comparaison avec année précédente

5. Alertes push :
   - Notification si détection d'opportunité
   - Email hebdomadaire "Vos insights de la semaine"

Utilise :
- Chart.js pour graphiques
- API OpenAI pour génération des recommandations
- Calculs statistiques en JS
- Design avec icônes d'alerte colorées (rouge/orange/vert)
```

---

## 🌐 5. MARKETPLACE & COMMUNAUTÉ INTÉGRÉE

### Concept
Transformer LiveOwnerUnit en écosystème avec services partenaires et entraide.

### Bénéfices
- **Revenus additionnels** : commission sur services tiers
- Fidélisation accrue : tout en un seul endroit
- **Effet réseau** : valeur augmente avec chaque nouvel utilisateur

### Fonctionnalités clés
- **Marketplace de services** :
  - 🧹 Sociétés de ménage vérifiées (prise de contact direct)
  - 📸 Photographes professionnels de gîtes
  - 🎨 Designers d'intérieur spécialisés
  - 🔧 Artisans/réparateurs (plombier, électricien)
  - 📦 Fournisseurs de produits d'accueil en gros
  - ⚖️ Experts-comptables spécialisés LMNP
- **Forum communauté** :
  - Questions/réponses entre propriétaires
  - Partage de bonnes pratiques
  - Entraide sur problèmes fiscaux, légaux
  - Système de réputation (likes, meilleure réponse)
- **Groupes locaux** : par région pour mutualiser
- **Events** : webinaires, formations en ligne
- **Offres négociées** : -20% chez partenaires pour membres

### 📝 Prompt Copilot
```
Développe une Marketplace + Forum communautaire pour LiveOwnerUnit.

Structure :

1. PAGE MARKETPLACE
Table "services_providers" :
- category (ménage, photo, comptable, artisan, fourniture)
- name, description, logo
- location, zone_intervention
- rating (note /5)
- nb_reviews
- tarifs
- contact (email, tel, site web)

Affichage :
- Filtres par catégorie et localisation
- Cartes de prestataires avec :
  * Logo + nom
  * Note étoiles + nombre d'avis
  * Prix indicatif
  * Bouton "Contacter"
  * Badge "Vérifié LiveOwnerUnit"
- Modal détail avec galerie photos, description complète

2. PAGE FORUM COMMUNAUTÉ
Table "forum_topics" :
- title, content, category
- author_id, created_at
- views, likes
- tags

Table "forum_replies" :
- topic_id, content, author_id
- is_best_answer (booléen)
- likes

Fonctionnalités :
- Création de topics avec catégories (Fiscal, Ménage, Marketing, Technique, Autre)
- Réponses avec éditeur Markdown
- Système de likes
- Marquer "Meilleure réponse"
- Notifications si réponse à votre topic
- Recherche dans le forum
- Classement membres par contributions

3. GROUPES LOCAUX
- Groupes privés par région (ex: "Propriétaires Provence")
- Chat de groupe
- Organisation d'événements locaux

Design :
- Style moderne type Reddit/Stack Overflow
- Badges de réputation utilisateurs
- Système de points communautaires
```

---


## 🏆 8. CHALLENGES & CONCOURS MENSUELS

### Concept
Compétitions amicales entre propriétaires pour booster l'engagement.

### Bénéfices
- **Utilisation plateforme +70%**
- Sentiment d'appartenance communautaire
- Viralité sur réseaux sociaux

### Fonctionnalités clés
- **Challenges thématiques** :
  - 📅 "Taux d'occupation de février" → Top 10 gagnent un prix
  - ⚡ "Temps de réponse clients" → Challenge réactivité
  - 💬 "Meilleurs avis clients" → Concours qualité service
  - 🌱 "Pratiques éco-responsables" → Gîte le plus vert
- **Leaderboards en temps réel**
- **Prix attractifs** :
  - 1er : 6 mois gratuits
  - 2e-3e : 3 mois gratuits
  - 4e-10e : 1 mois gratuit
  - Tous participants : badge spécial
- **Partage social** : "Je suis 3ème au challenge de février !"
- **Live events** : annonce des gagnants en vidéo

### 📝 Prompt Copilot
```
Crée un système de challenges et concours mensuels pour LiveOwnerUnit.

Structure :

1. TABLE "monthly_challenges"
- month_year (ex: "2026-02")
- challenge_type (occupation, reactivity, reviews, eco)
- title, description
- start_date, end_date
- rewards (JSON)
- status (upcoming, active, ended)

2. TABLE "challenge_participants"
- user_id, challenge_id
- score (calculé automatiquement)
- rank
- badge_earned

3. PAGE CHALLENGES
Vue d'ensemble :
- Challenge en cours avec :
  * Titre + description
  * Jours restants (compte à rebours)
  * Votre position actuelle : "Vous êtes 12ème avec 78% de TO"
  * Leaderboard top 10
  * Graphique d'évolution de votre score
- Challenges terminés (historique)
- Prochains challenges (teasing)

4. CALCUL AUTOMATIQUE DES SCORES
Exemples selon type :
- "Taux occupation" : (jours occupés / jours disponibles) * 100
- "Réactivité" : temps moyen de réponse en minutes
- "Avis clients" : note moyenne sur le mois
- "Eco-responsable" : points pour actions vertes (tri, économies énergie)

5. LEADERBOARD TEMPS RÉEL
- Tableau avec :
  * Position (#1, #2...)
  * Nom utilisateur (ou pseudo)
  * Avatar
  * Score
  * Badge si dans top 3
- Mise à jour toutes les heures
- Animation montée/descente positions

6. ANNONCE GAGNANTS
- Email aux participants en fin de mois
- Post blog "Les gagnants du challenge de février"
- Notification push avec confettis
- Attribution automatique des récompenses

7. PARTAGE SOCIAL
- Bouton "Partager ma position" avec image générée :
  * "Je suis 3ème au Challenge Février de LiveOwnerUnit !"
  * Logo + statistiques

Design :
- Style compétition sportive
- Couleurs podium : or, argent, bronze
- Animations de classement
- Trophées et médailles
```

---

## 💡 9. TEMPLATES & BIBLIOTHÈQUE DE RESSOURCES

### Concept
Marketplace de templates prêts à l'emploi créés par la communauté.

### Bénéfices
- **Gain de temps** : 80% pour création contenus
- Qualité professionnelle garantie
- Revenus additionnels (commissions)

### Fonctionnalités clés
- **Templates payants et gratuits** :
  - 📧 Messages types clients (50+ modèles)
  - 📋 Checklists ménage détaillées
  - 🎨 Designs de fiches clients
  - 📊 Règles fiscales pré-configurées
  - 🏡 Descriptions de gîtes optimisées SEO
- **Marketplace créateurs** :
  - Propriétaires peuvent vendre leurs templates
  - Commission 30% pour LiveOwnerUnit
  - Système d'avis et notes
- **Preview avant achat**
- **1-click install** : applique directement au gîte
- **Bibliothèque légale** : CGV, règlements intérieurs, contrats

### 📝 Prompt Copilot
```
Développe une Marketplace de templates pour LiveOwnerUnit.

Structure :

1. TABLE "templates"
- category (messages, checklists, designs, fiscal, legal)
- title, description, preview_image
- author_id (créateur)
- price (0 si gratuit)
- downloads_count
- rating, nb_reviews
- file_url ou content (JSON)
- is_premium (booléen)
- tags

2. PAGE MARKETPLACE
Interface :
- Filtres :
  * Catégorie
  * Prix (Gratuit / Payant)
  * Note minimale
  * Tendances
- Grille de templates avec cartes :
  * Image de preview
  * Titre + description courte
  * Prix ou "Gratuit"
  * Note étoiles
  * Nombre de téléchargements
  * Badge "Bestseller" si >100 téléchargements
  * Bouton "Aperçu" / "Acheter"

3. MODAL DÉTAILS TEMPLATE
- Galerie d'images/screenshots
- Description complète
- Avis utilisateurs
- "Créé par [nom]" avec lien profil
- Bouton "Acheter maintenant" ou "Télécharger gratuitement"
- Templates similaires en bas

4. PROCESSUS D'ACHAT
Si payant :
- Ajout au panier
- Paiement Stripe
- Téléchargement automatique
- Ajout à "Mes templates achetés"

Si gratuit :
- 1-click téléchargement
- Ajout à "Ma bibliothèque"

5. APPLICATION DU TEMPLATE
- Bouton "Utiliser ce template"
- Sélection du gîte cible
- Preview des changements
- Confirmation
- Application automatique

6. ESPACE CRÉATEUR
Pour vendre ses propres templates :
- Formulaire d'upload avec :
  * Catégorie
  * Titre, description
  * Images de preview
  * Prix suggéré
  * Fichier template
- Validation par modération LiveOwnerUnit
- Dashboard revenus :
  * Nombre de ventes
  * Revenus générés (70% pour créateur)
  * Statistiques

7. TEMPLATES PAR DÉFAUT
Inclure gratuitement :
- 10 messages types (bienvenue, arrivée, départ, etc.)
- 3 checklists ménage
- 2 designs de fiche client
- 1 règlement intérieur type

Design :
- Style marketplace moderne (Envato, Gumroad)
- Tags cliquables
- Search barre
- Icônes par catégorie
```

---

## 🎨 10. GÉNÉRATEUR DE CONTENU IA MULTILINGUE

### Concept
Création automatique de tous les contenus en 10 langues via IA.

### Bénéfices
- **Réservations internationales +45%**
- Gain de temps sur traduction
- SEO optimisé dans chaque langue

### Fonctionnalités clés
- **Générateur de descriptions** :
  - Description longue gîte (500 mots) optimisée SEO
  - Titres accrocheurs pour annonces
  - Meta descriptions pour site web
- **Traduction instantanée** :
  - 10 langues : FR, EN, ES, DE, IT, PT, NL, RU, ZH, JA
  - Ton adapté (professionnel / chaleureux / luxe)
- **Messages automatiques** :
  - Génération réponses FAQ
  - Messages de bienvenue personnalisés
  - Relances clients
- **Optimisation SEO** :
  - Mots-clés suggérés par région
  - Structure Hn optimale
  - Rich snippets automatiques

### 📝 Prompt Copilot
```
Crée un générateur de contenu IA multilingue pour LiveOwnerUnit.

Fonctionnalités :

1. GÉNÉRATEUR DE DESCRIPTION GÎTE
Interface :
- Formulaire de saisie :
  * Type de gîte (maison, appartement, château, etc.)
  * Nombre de pièces, capacité
  * Équipements principaux (sélection multiple)
  * Points forts (vue, piscine, proximité plage, etc.)
  * Région/ville
  * Ambiance souhaitée (rustique, moderne, luxe, familial)
- Langue cible (dropdown 10 langues)
- Longueur souhaitée (courte 100 mots / moyenne 300 / longue 500)
- Ton (professionnel / chaleureux / poétique / commercial)

Bouton "Générer avec l'IA" →
- Appel API Claude/ChatGPT avec prompt structuré :
  "Génère une description de gîte en [langue] pour un [type] de [capacité] personnes avec [équipements]. Ton [ton], longueur [X] mots. Optimisé SEO avec mots-clés locaux [région]. Inclus appel à l'action."

Affichage :
- Texte généré avec Markdown
- Bouton "Copier"
- Bouton "Régénérer" (variante)
- Bouton "Appliquer à mon gîte"
- Preview annonce avec description

2. TRADUCTEUR INSTANTANÉ
- Textarea avec texte source
- Sélection langue source et cible
- Bouton "Traduire"
- Adaptation culturelle (ex: $ → € si USD → EUR)
- Préservation des balises HTML

3. GÉNÉRATEUR DE MESSAGES CLIENTS
Templates IA :
- Message de bienvenue
- Instructions d'arrivée
- Demande d'avis après séjour
- Réponse à problème signalé
- Offre promotion retour client

Chaque template avec personnalisation :
- Nom du client
- Dates du séjour
- Nom du gîte
- Informations spécifiques

4. OPTIMISEUR SEO
Analyse du texte fourni et suggestions :
- Mots-clés manquants
- Structure Hn
- Longueur optimale
- Lisibilité
- Score SEO /100

5. BIBLIOTHÈQUE DE RÉSULTATS
- Sauvegarde automatique textes générés
- Historique avec possibilité de réutiliser

API :
- Utilise Anthropic Claude API ou OpenAI GPT-4
- Gestion des crédits : X générations/mois selon abonnement
- Option "crédits supplémentaires" à l'achat

Design :
- Interface type playground IA
- Preview en temps réel
- Animations de génération (wave)
- Compteur de mots
```

---

## 📈 RÉSUMÉ DES IMPACTS ATTENDUS

| Fonctionnalité | Engagement | Rétention | Viralité | Revenus |
|---|---|---|---|---|
| 🤖 Assistant IA | +40% | +35% | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| 👥 Parrainage | +30% | +60% | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 📊 Insights IA | +25% | +40% | ⭐⭐ | ⭐⭐⭐⭐ |
| 🌐 Marketplace | +50% | +70% | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 🎯 Onboarding | +60% | +80% | ⭐⭐ | ⭐⭐⭐ |
| 📱 Notifications | +40% | +45% | ⭐⭐ | ⭐⭐ |
| 🏆 Challenges | +70% | +55% | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| 💡 Templates | +35% | +50% | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| 🎨 IA Contenu | +45% | +40% | ⭐⭐⭐ | ⭐⭐⭐⭐ |

---

## 🚀 PLAN DE DÉPLOIEMENT SUGGÉRÉ

### Phase 1 - Quick Wins (1-2 mois)
1. **Gamification de base** (points, badges, niveaux)
2. **Programme de parrainage**
3. **Onboarding gamifié**

### Phase 2 - IA & Automation (2-3 mois)
4. **Assistant IA conversationnel**
5. **Générateur de contenu IA**
6. **Notifications push intelligentes**

### Phase 3 - Communauté & Marketplace (3-4 mois)
7. **Marketplace de services**
8. **Templates & bibliothèque**
9. **Forum communautaire**

### Phase 4 - Advanced (4-6 mois)
10. **Insights IA prédictifs**
11. **Challenges mensuels**
12. **Intégrations avancées**

---

## 💰 POTENTIEL DE REVENUS ADDITIONNELS

- **Commissions Marketplace** : 30% sur services = ~500€/mois/1000 users
- **Templates premium** : 20% ventes = ~300€/mois
- **Crédits IA supplémentaires** : 5€/100 générations = ~200€/mois/1000 users
- **Abonnements premium** : +15% conversion via features exclusives

**Total estimé** : +1000€/mois pour 1000 utilisateurs actifs

---

## 🎯 CONCLUSION

Ces 10 fonctionnalités transformeront **LiveOwnerUnit** en plateforme **virale et addictive** :

✅ **Engagement massif** via gamification et challenges  
✅ **Croissance organique** via parrainage viral  
✅ **Gain de temps** via automatisation IA  
✅ **Communauté forte** via marketplace et forum  
✅ **Revenus diversifiés** via commissions et premium  

**Objectif** : Passer de plateforme de gestion à **écosystème complet** incontournable pour propriétaires de gîtes ! 🚀
